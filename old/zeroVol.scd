		~ibeat.set(\volume, 0);
		~trigDrones.set(\volume, 0);
		~gran1.set( \volume, 0);
		~gran2.set( \volume, 0);

		~fw.set(\volume, 0);
		Ndef(\complexB).set(\volume, 0);
		~pad.set(\volume, 0);
		~stranga.set(\volume, 0);
		~string.set(\volume, 0);

~drC.set(\volume, 0);
~glitch_drones.set(\volume, 0);
name: Create release ZIP with submodules

on:
  release:
    types: [published]

permissions:
  contents: write  # This fixes the "resource not accessible" error

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository with submodules
        uses: actions/checkout@v4
        with:
          submodules: recursive
          fetch-depth: 0

      - name: Show submodule status (debugging)
        run: git submodule status

      - name: Create ZIP including submodules
        run: |
          zip -r full-release.zip . -x '*.git*'

      - name: Upload ZIP to GitHub release
        uses: softprops/action-gh-release@v1
        with:
          files: full-release.zip
        env:
          GITHUB_TOKEN: ${{ secrets.BUSCON_SOUNDINMOTION }}

